fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# 多 Agent 编排与 RAG 依赖
langgraph>=0.2.0
chromadb>=0.5.0
pydantic>=2.6.0

# Amazon Bedrock 真实实现依赖（LLM 与 embedding）
boto3>=1.34.0

# 从 .env 文件加载配置（各成员填自己的 AWS 凭证，密钥不进仓库）
python-dotenv>=1.0.0

# 测试依赖
pytest>=8.0.0
hypothesis>=6.100.0
httpx>=0.27.0

# 浏览器 agent(Nova 像人一样操作 Amazon)：安装后需 `python -m playwright install chromium`
playwright>=1.62.0
playwright-stealth>=2.0.0
